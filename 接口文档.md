# 当前项目接口文档

> 说明
> - 本文档基于当前仓库源码整理。
> - 默认服务地址按当前启动配置：`http://localhost:9090/api/ragent`
> - 如果你在别的主机或端口启动，请把域名、端口替换掉。
> - 下面的“路径”默认都需要再拼上上面的基础地址。

## 1. 项目范围

当前仓库对外的 HTTP 接口主要分为这些模块：

- `luoluo-system`
- `luoluo-member`
- `luoluo-knowledge`
- `luoluo-ingestion`
- `luoluo-rag`
- `luoluo-admin`
- `luoluo-rag-mcp`
- `luoluo-common` 提供统一返回、异常处理、登录校验等公共能力

从源码看，当前项目的主要 HTTP 入口都由 Spring MVC Controller 暴露，统一前缀大多是：

- ` /luoluo/member/...`
- ` /luoluo/system/public/...`
- ` /knowledge-base/...`
- ` /ingestion/...`
- ` /rag/...`
- ` /conversations/...`
- ` /mappings/...`
- ` /intent-tree/...`
- ` /sample-questions/...`
- ` /admin/dashboard/...`

---

## 2. 统一约定

### 2.1 鉴权约定

- `@SaIgnore`：公开接口，不需要登录态。
- `@MemberLoginRequired`：需要登录态，底层等价于 `@SaCheckLogin`。
- `UserController` 中的用户管理接口还会额外检查 `admin` 角色。
- 业务层依赖 `UserContext` 的接口，通常需要先登录再调用。

### 2.2 Content-Type

- JSON 接口：`Content-Type: application/json`
- 文件上传：`multipart/form-data`
- SSE 流式接口：`Accept: text/event-stream`

### 2.3 分页约定

项目里有两种分页风格：

- 直接继承 `com.baomidou.mybatisplus.extension.plugins.pagination.Page`
  - 常见字段：`current`、`size`
  - 还会携带业务筛选字段，比如 `keyword`、`status`、`enabled`、`traceId`
- 手工分页参数
  - 常见字段：`pageNo`、`pageSize`

### 2.4 统一返回包装

#### `Result<T>`

用于大部分内部接口。

字段如下：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | string | 成功时通常为 `"0"` |
| `message` | string | 提示信息 |
| `data` | T | 数据体 |
| `requestId` | string | 请求标识，部分场景可能为空 |

成功响应示例：

```json
{
  "code": "0",
  "message": "操作成功",
  "data": {}
}
```

#### `R<T>`

用于公开系统接口。

字段如下：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | int | `0` 表示成功，`1` 表示失败 |
| `message` | string | 提示信息 |
| `data` | T | 数据体 |

成功响应示例：

```json
{
  "code": 0,
  "message": "login success",
  "data": {}
}
```

### 2.5 常见错误码

| code | 含义 | 来源 |
| --- | --- | --- |
| `0` / `"0"` | 成功 | `Results.success()` / `R.ok()` |
| `1` | 失败 | `R.fail()` |
| `400` / `"400"` | 参数错误、校验失败 | 全局异常处理 |
| `401` / `"401"` | 未登录 | Sa-Token 异常处理 |
| `403` / `"403"` | 无权限 | Sa-Token 异常处理 |
| `500` / `"500"` | 系统异常 | 全局异常处理 |

---

## 3. 公共系统接口

### 3.1 图形验证码

#### `GET /luoluo/system/public/captcha/image`

- 是否公开：是
- 返回包装：`R<ImageCaptchaResponse>`

说明：

- 默认用于“发送短信/邮件验证码”前的人机校验。
- 如果配置关闭图形验证码，会返回空验证码对象。

返回对象 `ImageCaptchaResponse`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `captchaKey` | string | 验证码 key |
| `imageBase64` | string | SVG Base64 内容 |
| `expiresIn` | long | 有效期秒数 |

示例：

```json
{
  "code": 0,
  "message": "image captcha created",
  "data": {
    "captchaKey": "captcha_xxx",
    "imageBase64": "PHN2ZyB4bWxucz0i...",
    "expiresIn": 120
  }
}
```

### 3.2 公开会员登录/注册

#### `POST /luoluo/system/public/member/auth/login`

- 是否公开：是
- 返回包装：`R<MemberLoginResponse>`

#### `POST /luoluo/system/public/member/auth/register`

- 是否公开：是
- 返回包装：`R<MemberLoginResponse>`

#### `POST /luoluo/system/public/member/auth/code/send`

- 是否公开：是
- 返回包装：`R<MemberSendVerifyCodeResponse>`

请求体通用字段见下文“会员认证 DTO”。

备注：

- 公开接口和会员模块接口使用同一套 DTO。
- 公开接口返回的 `message` 分别是 `login success`、`register success`、`send code success`。

---

## 4. 会员模块

### 4.1 会员认证

#### `POST /luoluo/member/auth/login`

- 需要登录：否
- 返回包装：`Result<MemberLoginResponse>`

#### `POST /luoluo/member/auth/register`

- 需要登录：否
- 返回包装：`Result<MemberLoginResponse>`

#### `POST /luoluo/member/auth/logout`

- 需要登录：是
- 返回包装：`Result<Void>`

#### `POST /luoluo/member/auth/send-code`

- 需要登录：否
- 返回包装：`Result<MemberSendVerifyCodeResponse>`

### 4.2 当前用户资料

#### `GET /luoluo/member/profile/me`

- 需要登录：是
- 返回包装：`Result<MemberProfileResponse>`

### 4.3 管理用户

#### `GET /user/me`

- 需要登录：是
- 返回包装：`Result<CurrentUserVO>`

#### `GET /users`

- 需要登录：是
- 额外要求：`admin` 角色
- 返回包装：`Result<IPage<UserVO>>`

#### `POST /users`

- 需要登录：是
- 额外要求：`admin` 角色
- 返回包装：`Result<String>`

#### `PUT /users/{id}`

- 需要登录：是
- 额外要求：`admin` 角色
- 返回包装：`Result<Void>`

#### `DELETE /users/{id}`

- 需要登录：是
- 额外要求：`admin` 角色
- 返回包装：`Result<Void>`

#### `PUT /user/password`

- 需要登录：是
- 返回包装：`Result<Void>`

---

## 5. 知识库模块

### 5.1 知识库管理

#### `POST /knowledge-base`

- 返回包装：`Result<String>`
- 说明：创建知识库

#### `PUT /knowledge-base/{kb-id}`

- 返回包装：`Result<Void>`
- 说明：更新知识库名称或嵌入模型

#### `DELETE /knowledge-base/{kb-id}`

- 返回包装：`Result<Void>`
- 说明：删除知识库

#### `GET /knowledge-base/{kb-id}`

- 返回包装：`Result<KnowledgeBaseVO>`
- 说明：按 ID 查询知识库

#### `GET /knowledge-base`

- 返回包装：`Result<IPage<KnowledgeBaseVO>>`
- 说明：分页查询知识库

#### `GET /knowledge-base/chunk-strategies`

- 返回包装：`Result<List<ChunkStrategyVO>>`
- 说明：列出可见的切分策略

### 5.2 文档管理

#### `POST /knowledge-base/{kb-id}/docs/upload`

- 请求类型：`multipart/form-data`
- 返回包装：`Result<KnowledgeDocumentVO>`

表单字段：

- `file`：上传文件，可选但实际业务通常需要传
- `sourceType`
- `sourceLocation`
- `scheduleEnabled`
- `scheduleCron`
- `processMode`
- `chunkStrategy`
- `chunkConfig`
- `pipelineId`

#### `POST /knowledge-base/docs/{doc-id}/chunk`

- 返回包装：`Result<Void>`
- 说明：对文档发起切分任务

#### `DELETE /knowledge-base/docs/{doc-id}`

- 返回包装：`Result<Void>`

#### `GET /knowledge-base/docs/{doc-id}`

- 返回包装：`Result<KnowledgeDocumentVO>`

#### `PUT /knowledge-base/docs/{doc-id}`

- 返回包装：`Result<Void>`

#### `GET /knowledge-base/{kb-id}/docs`

- 返回包装：`Result<IPage<KnowledgeDocumentVO>>`
- 说明：按知识库分页列出文档

#### `GET /knowledge-base/docs/search`

- 返回包装：`Result<List<KnowledgeDocumentSearchVO>>`
- 查询参数：
  - `keyword`，可选
  - `limit`，默认 `8`

#### `PATCH /knowledge-base/docs/{doc-id}/enable`

- 返回包装：`Result<Void>`
- 查询参数：
  - `value`：`true` / `false`

#### `GET /knowledge-base/docs/{doc-id}/chunk-logs`

- 返回包装：`Result<IPage<KnowledgeDocumentChunkLogVO>>`

### 5.3 Chunk 管理

#### `GET /knowledge-base/docs/{doc-id}/chunks`

- 返回包装：`Result<IPage<KnowledgeChunkVO>>`

#### `POST /knowledge-base/docs/{doc-id}/chunks`

- 返回包装：`Result<KnowledgeChunkVO>`

#### `PUT /knowledge-base/docs/{doc-id}/chunks/{chunk-id}`

- 返回包装：`Result<Void>`

#### `DELETE /knowledge-base/docs/{doc-id}/chunks/{chunk-id}`

- 返回包装：`Result<Void>`

#### `PATCH /knowledge-base/docs/{doc-id}/chunks/{chunk-id}/enable`

- 返回包装：`Result<Void>`
- 查询参数：
  - `value`：`true` / `false`

#### `PATCH /knowledge-base/docs/{doc-id}/chunks/batch-enable`

- 返回包装：`Result<Void>`
- 查询参数：
  - `value`：`true` / `false`
- 请求体：
  - 可选 `KnowledgeChunkBatchRequest`

---

## 6. 任务与流水线模块

### 6.1 Ingestion Pipeline

#### `POST /ingestion/pipelines`

- 返回包装：`Result<IngestionPipelineVO>`

#### `PUT /ingestion/pipelines/{id}`

- 返回包装：`Result<IngestionPipelineVO>`

#### `GET /ingestion/pipelines/{id}`

- 返回包装：`Result<IngestionPipelineVO>`

#### `GET /ingestion/pipelines`

- 返回包装：`Result<IPage<IngestionPipelineVO>>`
- 查询参数：
  - `pageNo`，默认 `1`
  - `pageSize`，默认 `10`
  - `keyword`，可选

#### `DELETE /ingestion/pipelines/{id}`

- 返回包装：`Result<Void>`

### 6.2 Ingestion Task

#### `POST /ingestion/tasks`

- 返回包装：`Result<IngestionResult>`

#### `POST /ingestion/tasks/upload`

- 请求类型：`multipart/form-data`
- 返回包装：`Result<IngestionResult>`
- 参数：
  - `pipelineId`：必填
  - `file`：必填

#### `GET /ingestion/tasks/{id}`

- 返回包装：`Result<IngestionTaskVO>`

#### `GET /ingestion/tasks/{id}/nodes`

- 返回包装：`Result<List<IngestionTaskNodeVO>>`

#### `GET /ingestion/tasks`

- 返回包装：`Result<IPage<IngestionTaskVO>>`
- 查询参数：
  - `pageNo`，默认 `1`
  - `pageSize`，默认 `10`
  - `status`，可选

---

## 7. RAG 模块

### 7.1 系统设置

#### `GET /rag/settings`

- 返回包装：`Result<SystemSettingsVO>`

说明：

- 返回系统级 RAG 配置、AI 配置、上传限制。
- 其中 AI Provider 的 `apiKey` 会做掩码处理。

### 7.2 流式聊天

#### `GET /rag/v3/chat`

- 返回类型：`SseEmitter`
- 响应类型：`text/event-stream;charset=UTF-8`

查询参数：

- `question`：必填
- `conversationId`：可选
- `deepThinking`：可选，默认 `false`

说明：

- 这是核心对话入口。
- 如果没有传 `conversationId`，后端会自动生成新的会话 ID。
- 当前接口带有防重复提交逻辑。
- 业务上会读取当前登录用户信息，建议在登录后调用。

#### `POST /rag/v3/stop`

- 返回包装：`Result<Void>`
- 查询参数：
  - `taskId`：必填

### 7.3 会话管理

#### `GET /conversations`

- 返回包装：`Result<List<ConversationVO>>`
- 说明：返回当前用户的会话列表

#### `PUT /conversations/{conversationId}`

- 返回包装：`Result<Void>`

#### `DELETE /conversations/{conversationId}`

- 返回包装：`Result<Void>`

#### `GET /conversations/{conversationId}/messages`

- 返回包装：`Result<List<ConversationMessageVO>>`

### 7.4 消息反馈

#### `POST /conversations/messages/{messageId}/feedback`

- 返回包装：`Result<Void>`

### 7.5 词条映射

#### `GET /mappings`

- 返回包装：`Result<IPage<QueryTermMappingVO>>`

#### `GET /mappings/{id}`

- 返回包装：`Result<QueryTermMappingVO>`

#### `POST /mappings`

- 返回包装：`Result<String>`

#### `PUT /mappings/{id}`

- 返回包装：`Result<Void>`

#### `DELETE /mappings/{id}`

- 返回包装：`Result<Void>`

### 7.6 意图树

#### `GET /intent-tree/trees`

- 返回包装：`Result<List<IntentNodeTreeVO>>`

#### `POST /intent-tree`

- 返回包装：`Result<String>`

#### `PUT /intent-tree/{id}`

- 返回体：无
- 说明：接口方法返回 `void`

#### `DELETE /intent-tree/{id}`

- 返回体：无

#### `POST /intent-tree/batch/enable`

- 返回体：无

#### `POST /intent-tree/batch/disable`

- 返回体：无

#### `POST /intent-tree/batch/delete`

- 返回体：无

### 7.7 示例问题

#### `GET /rag/sample-questions`

- 返回包装：`Result<List<SampleQuestionVO>>`
- 说明：随机返回一批示例问题

#### `GET /sample-questions`

- 返回包装：`Result<IPage<SampleQuestionVO>>`

#### `GET /sample-questions/{id}`

- 返回包装：`Result<SampleQuestionVO>`

#### `POST /sample-questions`

- 返回包装：`Result<String>`

#### `PUT /sample-questions/{id}`

- 返回包装：`Result<Void>`

#### `DELETE /sample-questions/{id}`

- 返回包装：`Result<Void>`

### 7.8 追踪查询

#### `GET /rag/traces/runs`

- 返回包装：`Result<IPage<RagTraceRunVO>>`

#### `GET /rag/traces/runs/{traceId}`

- 返回包装：`Result<RagTraceDetailVO>`

#### `GET /rag/traces/runs/{traceId}/nodes`

- 返回包装：`Result<List<RagTraceNodeVO>>`

---

## 8. 管理仪表盘

### `GET /admin/dashboard/overview`

- 返回包装：`Result<DashboardOverviewVO>`
- 查询参数：
  - `window`，可选

### `GET /admin/dashboard/performance`

- 返回包装：`Result<DashboardPerformanceVO>`
- 查询参数：
  - `window`，可选

### `GET /admin/dashboard/trends`

- 返回包装：`Result<DashboardTrendsVO>`
- 查询参数：
  - `metric`，必填
  - `window`，可选
  - `granularity`，可选

---

## 9. 会员认证 DTO

### 9.1 `MemberLoginRequest`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `grantType` | string | 是 | 登录方式，通常为 `password`、`sms`、`email` |
| `deviceType` | string | 否 | 设备类型 |
| `username` | string | 否 | 用户名登录 |
| `password` | string | 否 | 密码 |
| `phone` | string | 否 | 手机号登录 |
| `smsCode` | string | 否 | 短信验证码 |
| `email` | string | 否 | 邮箱登录 |
| `emailCode` | string | 否 | 邮箱验证码 |

### 9.2 `MemberRegisterRequest`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `grantType` | string | 是 | 注册方式，通常为 `password`、`sms`、`email` |
| `deviceType` | string | 否 | 设备类型 |
| `username` | string | 否 | 用户名 |
| `password` | string | 否 | 密码 |
| `confirmPassword` | string | 否 | 确认密码 |
| `phone` | string | 否 | 手机号 |
| `smsCode` | string | 否 | 短信验证码 |
| `email` | string | 否 | 邮箱 |
| `emailCode` | string | 否 | 邮箱验证码 |
| `displayName` | string | 否 | 显示名 |

### 9.3 `MemberSendVerifyCodeRequest`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `grantType` | string | 是 | 发送验证码类型，通常为 `sms` 或 `email` |
| `bizType` | string | 否 | 业务类型 |
| `captchaKey` | string | 否 | 图形验证码 key |
| `captchaCode` | string | 否 | 图形验证码内容 |
| `phone` | string | 否 | 手机号 |
| `email` | string | 否 | 邮箱 |

### 9.4 `MemberLoginResponse`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `token` | string | 登录 token |
| `tokenType` | string | token 类型，通常为 `Bearer` |
| `expiresIn` | long | 过期秒数 |
| `grantType` | string | 当前登录方式 |
| `user` | `MemberUserSummary` | 用户摘要 |

### 9.5 `MemberSendVerifyCodeResponse`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `requestId` | string | 本次发送请求流水 |
| `grantType` | string | 发送方式 |
| `target` | string | 发送目标，手机号或邮箱脱敏值 |
| `expiresIn` | long | 验证码有效期秒数 |
| `issuedCode` | string | 调试用明文验证码，正式环境通常不建议依赖 |

### 9.6 `MemberUserSummary`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | long | 用户 ID |
| `username` | string | 用户名 |
| `displayName` | string | 显示名 |
| `phone` | string | 手机号 |
| `email` | string | 邮箱 |
| `userType` | string | 用户类型 |

### 9.7 `MemberProfileResponse`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | long | 用户 ID |
| `username` | string | 用户名 |
| `displayName` | string | 显示名 |
| `phone` | string | 手机号 |
| `email` | string | 邮箱 |
| `userType` | string | 用户类型 |
| `status` | string | 状态 |

### 9.8 `ImageCaptchaResponse`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `captchaKey` | string | 验证码 key |
| `imageBase64` | string | Base64 图片内容 |
| `expiresIn` | long | 有效期秒数 |

---

## 10. 用户管理 DTO

### 10.1 `CurrentUserVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `userId` | string | 当前登录用户 ID |
| `username` | string | 用户名 |
| `role` | string | 角色 |
| `avatar` | string | 头像 |

### 10.2 `UserCreateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `username` | string | 用户名 |
| `password` | string | 密码 |
| `role` | string | 角色 |
| `avatar` | string | 头像 |

### 10.3 `UserUpdateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `username` | string | 用户名 |
| `password` | string | 密码 |
| `role` | string | 角色 |
| `avatar` | string | 头像 |

### 10.4 `UserPageRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `current` | long | 当前页，继承自 `Page` |
| `size` | long | 每页条数，继承自 `Page` |
| `keyword` | string | 关键词 |

### 10.5 `UserVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 用户 ID |
| `username` | string | 用户名 |
| `role` | string | 角色 |
| `avatar` | string | 头像 |
| `createTime` | `LocalDateTime` | 创建时间 |
| `updateTime` | `LocalDateTime` | 更新时间 |

### 10.6 `ChangePasswordRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `currentPassword` | string | 旧密码 |
| `newPassword` | string | 新密码 |

---

## 11. 知识库 DTO

### 11.1 `KnowledgeBaseCreateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | string | 知识库名称 |
| `embeddingModel` | string | 嵌入模型 |
| `collectionName` | string | 集合名 |

### 11.2 `KnowledgeBaseUpdateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 知识库 ID |
| `name` | string | 知识库名称 |
| `embeddingModel` | string | 嵌入模型 |

### 11.3 `KnowledgeBasePageRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `current` | long | 当前页 |
| `size` | long | 每页条数 |
| `name` | string | 名称过滤 |

### 11.4 `KnowledgeBaseVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 知识库 ID |
| `name` | string | 名称 |
| `embeddingModel` | string | 嵌入模型 |
| `collectionName` | string | 集合名 |
| `documentCount` | long | 文档数 |
| `createdBy` | string | 创建人 |
| `createTime` | date | 创建时间 |
| `updateTime` | date | 更新时间 |

### 11.5 `ChunkStrategyVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `value` | string | 策略值 |
| `label` | string | 策略名称 |
| `defaultConfig` | map | 默认配置 |

### 11.6 `KnowledgeDocumentUploadRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `sourceType` | string | 来源类型，通常为 `file`、`url`、`feishu`、`s3` |
| `sourceLocation` | string | 来源地址 |
| `scheduleEnabled` | boolean | 是否启用定时 |
| `scheduleCron` | string | 定时表达式 |
| `processMode` | string | 处理模式 |
| `chunkStrategy` | string | 切分策略 |
| `chunkConfig` | string | 切分配置 |
| `pipelineId` | string | 流水线 ID |

### 11.7 `KnowledgeDocumentUpdateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `docName` | string | 文档名 |
| `processMode` | string | 处理模式 |
| `chunkStrategy` | string | 切分策略 |
| `chunkConfig` | string | 切分配置 |
| `pipelineId` | string | 流水线 ID |
| `sourceLocation` | string | 来源地址 |
| `scheduleEnabled` | boolean | 是否启用定时 |
| `scheduleCron` | string | 定时表达式 |

### 11.8 `KnowledgeDocumentPageRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `current` | long | 当前页 |
| `size` | long | 每页条数 |
| `status` | string | 状态 |
| `keyword` | string | 关键词 |

### 11.9 `KnowledgeDocumentVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 文档 ID |
| `kbId` | string | 知识库 ID |
| `docName` | string | 文档名 |
| `sourceType` | string | 来源类型 |
| `sourceLocation` | string | 来源地址 |
| `scheduleEnabled` | integer | 是否启用定时 |
| `scheduleCron` | string | 定时表达式 |
| `enabled` | boolean | 是否启用 |
| `chunkCount` | integer | chunk 数量 |
| `fileUrl` | string | 文件 URL |
| `fileType` | string | 文件类型 |
| `fileSize` | long | 文件大小 |
| `chunkStrategy` | string | 切分策略 |
| `processMode` | string | 处理模式 |
| `chunkConfig` | string | 切分配置 |
| `pipelineId` | string | 流水线 ID |
| `status` | string | 状态 |
| `createdBy` | string | 创建人 |
| `updatedBy` | string | 更新人 |
| `createTime` | `LocalDateTime` | 创建时间 |
| `updateTime` | `LocalDateTime` | 更新时间 |

### 11.10 `KnowledgeDocumentSearchVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 文档 ID |
| `kbId` | string | 知识库 ID |
| `docName` | string | 文档名 |
| `kbName` | string | 知识库名 |

### 11.11 `KnowledgeDocumentChunkLogVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 日志 ID |
| `docId` | string | 文档 ID |
| `status` | string | 状态 |
| `processMode` | string | 处理模式 |
| `chunkStrategy` | string | 切分策略 |
| `pipelineId` | string | 流水线 ID |
| `pipelineName` | string | 流水线名 |
| `durationMs` | long | 耗时 |
| `extractDuration` | long | 抽取耗时 |
| `chunkDuration` | long | 切分耗时 |
| `embedDuration` | long | 向量化耗时 |
| `persistDuration` | long | 持久化耗时 |
| `otherDuration` | long | 其他耗时 |
| `totalDuration` | long | 总耗时 |
| `chunkCount` | integer | chunk 数 |
| `message` | string | 消息 |
| `remark` | string | 备注 |
| `errorMessage` | string | 错误信息 |
| `startTime` | date | 开始时间 |
| `endTime` | date | 结束时间 |
| `createTime` | date | 创建时间 |

### 11.12 `KnowledgeChunkCreateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `content` | string | chunk 内容 |
| `index` | integer | chunk 序号 |
| `chunkId` | string | 外部 chunk ID |

### 11.13 `KnowledgeChunkUpdateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `content` | string | chunk 内容 |

### 11.14 `KnowledgeChunkBatchRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `chunkIds` | list<string> | chunk ID 列表 |

### 11.15 `KnowledgeChunkPageRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `current` | long | 当前页 |
| `size` | long | 每页条数 |
| `enabled` | integer | 启用状态 |

### 11.16 `KnowledgeChunkVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | chunk ID |
| `kbId` | string | 知识库 ID |
| `docId` | string | 文档 ID |
| `chunkIndex` | integer | chunk 序号 |
| `content` | string | 内容 |
| `contentHash` | string | 内容哈希 |
| `charCount` | integer | 字符数 |
| `tokenCount` | integer | token 数 |
| `enabled` | integer | 是否启用 |
| `createTime` | `LocalDateTime` | 创建时间 |
| `updateTime` | `LocalDateTime` | 更新时间 |

---

## 12. Ingestion DTO

### 12.1 `IngestionPipelineCreateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | string | 流水线名 |
| `description` | string | 描述 |
| `nodes` | list | 节点列表 |

### 12.2 `IngestionPipelineUpdateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | string | 流水线名 |
| `description` | string | 描述 |
| `nodes` | list | 节点列表 |

### 12.3 `IngestionPipelineNodeRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `nodeId` | string | 节点 ID |
| `nodeType` | string | 节点类型 |
| `settings` | json | 节点设置 |
| `condition` | json | 条件配置 |
| `nextNodeId` | string | 下一个节点 ID |

### 12.4 `IngestionTaskCreateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `pipelineId` | string | 流水线 ID |
| `source` | `DocumentSourceRequest` | 文档来源 |
| `metadata` | map | 额外元数据 |
| `vectorSpaceId` | `VectorSpaceId` | 向量空间标识 |

### 12.5 `DocumentSourceRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `type` | `SourceType` | 来源类型 |
| `location` | string | 来源地址 |
| `fileName` | string | 文件名 |
| `credentials` | map<string,string> | 凭据 |

### 12.6 `VectorSpaceId`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `logicalName` | string | 逻辑名称 |
| `namespace` | string | 命名空间 |

### 12.7 `IngestionPipelineVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 流水线 ID |
| `name` | string | 名称 |
| `description` | string | 描述 |
| `createdBy` | string | 创建人 |
| `nodes` | list | 节点 |
| `createTime` | date | 创建时间 |
| `updateTime` | date | 更新时间 |

### 12.8 `IngestionPipelineNodeVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 节点记录 ID |
| `nodeId` | string | 节点 ID |
| `nodeType` | string | 节点类型 |
| `settings` | json | 节点设置 |
| `condition` | json | 条件配置 |
| `nextNodeId` | string | 下一个节点 ID |

### 12.9 `IngestionTaskVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 任务 ID |
| `pipelineId` | string | 流水线 ID |
| `sourceType` | string | 来源类型 |
| `sourceLocation` | string | 来源地址 |
| `sourceFileName` | string | 来源文件名 |
| `status` | string | 状态 |
| `chunkCount` | integer | chunk 数 |
| `errorMessage` | string | 错误信息 |
| `logs` | list | 节点日志 |
| `metadata` | map | 元数据 |
| `startedAt` | date | 开始时间 |
| `completedAt` | date | 完成时间 |
| `createdBy` | string | 创建人 |
| `createTime` | date | 创建时间 |
| `updateTime` | date | 更新时间 |

### 12.10 `IngestionTaskNodeVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 节点记录 ID |
| `taskId` | string | 任务 ID |
| `pipelineId` | string | 流水线 ID |
| `nodeId` | string | 节点 ID |
| `nodeType` | string | 节点类型 |
| `nodeOrder` | integer | 节点顺序 |
| `status` | string | 状态 |
| `durationMs` | long | 耗时 |
| `message` | string | 消息 |
| `errorMessage` | string | 错误信息 |
| `output` | map | 输出 |
| `createTime` | date | 创建时间 |
| `updateTime` | date | 更新时间 |

### 12.11 `IngestionResult`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `taskId` | string | 任务 ID |
| `pipelineId` | string | 流水线 ID |
| `status` | enum | 任务状态，见 `IngestionStatus` |
| `chunkCount` | integer | chunk 数 |
| `message` | string | 返回消息 |

### 12.12 `IngestionStatus`

| 值 | 说明 |
| --- | --- |
| `pending` | 待处理 |
| `running` | 处理中 |
| `failed` | 失败 |
| `completed` | 完成 |

---

## 13. RAG DTO

### 13.1 `ConversationUpdateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `title` | string | 会话标题 |

### 13.2 `ConversationCreateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `conversationId` | string | 会话 ID |
| `userId` | string | 用户 ID |
| `question` | string | 首条问题 |
| `lastTime` | date | 最近时间 |

### 13.3 `ConversationVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `conversationId` | string | 会话 ID |
| `title` | string | 标题 |
| `lastTime` | date | 最近时间 |

### 13.4 `ConversationMessageVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 消息 ID |
| `conversationId` | string | 会话 ID |
| `role` | string | 角色 |
| `content` | string | 消息内容 |
| `thinkingContent` | string | 思考内容 |
| `thinkingDuration` | integer | 思考时长 |
| `vote` | integer | 投票 |
| `createTime` | date | 创建时间 |

### 13.5 `MessageFeedbackRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `vote` | integer | 反馈值 |
| `reason` | string | 原因 |
| `comment` | string | 备注 |

### 13.6 `QueryTermMappingCreateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `sourceTerm` | string | 源词 |
| `targetTerm` | string | 目标词 |
| `matchType` | integer | 匹配类型 |
| `priority` | integer | 优先级 |
| `enabled` | boolean | 是否启用 |
| `remark` | string | 备注 |

### 13.7 `QueryTermMappingUpdateRequest`

字段同 `QueryTermMappingCreateRequest`

### 13.8 `QueryTermMappingPageRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `current` | long | 当前页 |
| `size` | long | 每页条数 |
| `keyword` | string | 关键词 |

### 13.9 `QueryTermMappingVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | ID |
| `sourceTerm` | string | 源词 |
| `targetTerm` | string | 目标词 |
| `matchType` | integer | 匹配类型 |
| `priority` | integer | 优先级 |
| `enabled` | boolean | 是否启用 |
| `remark` | string | 备注 |
| `createTime` | date | 创建时间 |
| `updateTime` | date | 更新时间 |

### 13.10 `IntentNodeCreateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `kbId` | string | 知识库 ID |
| `intentCode` | string | 意图编码 |
| `name` | string | 名称 |
| `level` | integer | 层级 |
| `parentCode` | string | 父级编码 |
| `description` | string | 描述 |
| `examples` | list<string> | 示例问题 |
| `mcpToolId` | string | MCP 工具 ID |
| `topK` | integer | topK |
| `kind` | integer | 类型 |
| `sortOrder` | integer | 排序 |
| `enabled` | integer | 是否启用 |
| `promptSnippet` | string | 提示词片段 |
| `promptTemplate` | string | 提示词模板 |
| `paramPromptTemplate` | string | 参数提示词模板 |

### 13.11 `IntentNodeUpdateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | string | 名称 |
| `level` | integer | 层级 |
| `parentCode` | string | 父级编码 |
| `description` | string | 描述 |
| `examples` | list<string> | 示例问题 |
| `collectionName` | string | 集合名 |
| `mcpToolId` | string | MCP 工具 ID |
| `topK` | integer | topK |
| `kind` | integer | 类型 |
| `sortOrder` | integer | 排序 |
| `enabled` | integer | 是否启用 |
| `promptSnippet` | string | 提示词片段 |
| `promptTemplate` | string | 提示词模板 |
| `paramPromptTemplate` | string | 参数提示词模板 |

### 13.12 `IntentNodeBatchRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `ids` | list<string> | ID 列表 |

### 13.13 `IntentNodeTreeVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | ID |
| `intentCode` | string | 意图编码 |
| `name` | string | 名称 |
| `level` | integer | 层级 |
| `parentCode` | string | 父级编码 |
| `description` | string | 描述 |
| `examples` | string | 示例 |
| `collectionName` | string | 集合名 |
| `topK` | integer | topK |
| `kind` | integer | 类型 |
| `sortOrder` | integer | 排序 |
| `enabled` | integer | 是否启用 |
| `mcpToolId` | string | MCP 工具 ID |
| `promptSnippet` | string | 提示词片段 |
| `promptTemplate` | string | 提示词模板 |
| `paramPromptTemplate` | string | 参数提示词模板 |
| `children` | list | 子节点 |

### 13.14 `SampleQuestionCreateRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `title` | string | 标题 |
| `description` | string | 描述 |
| `question` | string | 问题 |

### 13.15 `SampleQuestionUpdateRequest`

字段同 `SampleQuestionCreateRequest`

### 13.16 `SampleQuestionPageRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `current` | long | 当前页 |
| `size` | long | 每页条数 |
| `keyword` | string | 关键词 |

### 13.17 `SampleQuestionVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | ID |
| `title` | string | 标题 |
| `description` | string | 描述 |
| `question` | string | 问题 |
| `createTime` | date | 创建时间 |
| `updateTime` | date | 更新时间 |

### 13.18 `RagTraceRunPageRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `current` | long | 当前页 |
| `size` | long | 每页条数 |
| `traceId` | string | Trace ID |
| `conversationId` | string | 会话 ID |
| `taskId` | string | 任务 ID |
| `status` | string | 状态 |

### 13.19 `RagTraceRunVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `traceId` | string | Trace ID |
| `traceName` | string | Trace 名称 |
| `entryMethod` | string | 入口方法 |
| `conversationId` | string | 会话 ID |
| `taskId` | string | 任务 ID |
| `userId` | string | 用户 ID |
| `username` | string | 用户名 |
| `status` | string | 状态 |
| `errorMessage` | string | 错误信息 |
| `durationMs` | long | 耗时 |
| `startTime` | date | 开始时间 |
| `endTime` | date | 结束时间 |

### 13.20 `RagTraceDetailVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `run` | `RagTraceRunVO` | Trace 运行信息 |
| `nodes` | list | 节点列表 |

### 13.21 `RagTraceNodeVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `traceId` | string | Trace ID |
| `nodeId` | string | 节点 ID |
| `parentNodeId` | string | 父节点 ID |
| `depth` | integer | 深度 |
| `nodeType` | string | 节点类型 |
| `nodeName` | string | 节点名称 |
| `className` | string | 类名 |
| `methodName` | string | 方法名 |
| `status` | string | 状态 |
| `errorMessage` | string | 错误信息 |
| `durationMs` | long | 耗时 |
| `startTime` | date | 开始时间 |
| `endTime` | date | 结束时间 |

---

## 14. RAG 设置与仪表盘 DTO

### 14.1 `SystemSettingsVO`

`SystemSettingsVO` 是一个组合对象，结构如下：

- `rag`
  - `default`
    - `collectionName`
    - `dimension`
    - `metricType`
  - `queryRewrite`
    - `enabled`
    - `maxHistoryMessages`
    - `maxHistoryChars`
  - `rateLimit.global`
    - `enabled`
    - `maxConcurrent`
    - `maxWaitSeconds`
    - `leaseSeconds`
    - `pollIntervalMs`
  - `memory`
    - `historyKeepTurns`
    - `summaryEnabled`
    - `summaryStartTurns`
    - `ttlMinutes`
    - `summaryMaxChars`
    - `titleMaxLength`
- `ai`
  - `providers`，key 为 provider 名称
    - `url`
    - `apiKey`
    - `endpoints`
  - `chat`
  - `embedding`
  - `rerank`
  - `selection`
    - `failureThreshold`
    - `openDurationMs`
  - `stream`
    - `messageChunkSize`
- `upload`
  - `maxFileSize`
  - `maxRequestSize`

### 14.2 `DashboardOverviewVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `window` | string | 时间窗 |
| `compareWindow` | string | 对比时间窗 |
| `updatedAt` | long | 更新时间戳 |
| `kpis` | `DashboardOverviewGroupVO` | KPI 分组 |

### 14.3 `DashboardOverviewGroupVO`

包含：

- `totalUsers`
- `activeUsers`
- `totalSessions`
- `sessions24h`
- `totalMessages`
- `messages24h`

每一项类型都是 `DashboardOverviewKpiVO`

### 14.4 `DashboardOverviewKpiVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `value` | long | 当前值 |
| `delta` | long | 增量 |
| `deltaPct` | double | 增长率 |

### 14.5 `DashboardPerformanceVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `window` | string | 时间窗 |
| `avgLatencyMs` | long | 平均耗时 |
| `p95LatencyMs` | long | P95 耗时 |
| `successRate` | double | 成功率 |
| `errorRate` | double | 错误率 |
| `noDocRate` | double | 无命中率 |
| `slowRate` | double | 慢请求率 |

### 14.6 `DashboardTrendsVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `metric` | string | 指标名 |
| `window` | string | 时间窗 |
| `granularity` | string | 粒度 |
| `series` | list | 曲线序列 |

### 14.7 `DashboardTrendSeriesVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | string | 序列名 |
| `data` | list | 数据点 |

### 14.8 `DashboardTrendPointVO`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `ts` | long | 时间戳 |
| `value` | double | 数值 |

---

## 15. SSE 流式事件说明

`GET /rag/v3/chat` 返回的是 SSE 流，不是一次性 JSON。

### 15.1 事件名

| event | 说明 | data 类型 |
| --- | --- | --- |
| `meta` | 返回会话和任务元信息 | `MetaPayload` |
| `message` | 流式消息片段 | `MessageDelta` |
| `finish` | 会话完成，返回 messageId 和 title | `CompletionPayload` |
| `done` | 流结束标记 | 字符串 `[DONE]` |
| `cancel` | 取消流式任务时发送 | `CompletionPayload` |
| `reject` | 排队超时或限流拒绝时发送 | `MessageDelta` |

### 15.2 载荷结构

#### `MetaPayload`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `conversationId` | string | 会话 ID |
| `taskId` | string | 任务 ID |

#### `MessageDelta`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `type` | string | 片段类型，常见值：`think`、`response` |
| `delta` | string | 本次增量内容 |

#### `CompletionPayload`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `messageId` | string | 助手消息 ID |
| `title` | string | 会话标题 |

### 15.3 前端处理建议

- 先监听 `meta`，拿到 `conversationId` 和 `taskId`。
- `message` 事件需要按 `type` 分流：
  - `think`：渲染思考区
  - `response`：渲染回答区
- 收到 `finish` 后，可以更新会话列表和消息 ID。
- 收到 `done` 后关闭流。
- 收到 `reject` 时通常表示排队超时或全局限流。
- 收到 `cancel` 时表示任务被主动停止。

---

## 16. 调用示例

### 16.1 登录

```bash
curl -X POST "http://localhost:9090/api/ragent/luoluo/member/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "grantType": "password",
    "deviceType": "web",
    "email": "demo@example.com",
    "password": "123456"
  }'
```

### 16.2 获取验证码图片

```bash
curl "http://localhost:9090/api/ragent/luoluo/system/public/captcha/image"
```

### 16.3 SSE 聊天

```bash
curl -N "http://localhost:9090/api/ragent/rag/v3/chat?question=你好&deepThinking=false"
```

### 16.4 上传知识库文档

```bash
curl -X POST "http://localhost:9090/api/ragent/knowledge-base/kb-1/docs/upload" \
  -F "file=@./demo.pdf" \
  -F "sourceType=file" \
  -F "sourceLocation=local" \
  -F "processMode=auto" \
  -F "chunkStrategy=default" \
  -F "chunkConfig={\"size\":512}" \
  -F "pipelineId=pipeline-1"
```

---

## 17. 备注

- 文档里列出的路径均基于当前源码中的 Controller 映射。
- `knowledge` 和 `admin` 子模块的启动配置都包含 `server.servlet.context-path: /api/ragent`。
- 如果你只是在本地 MockMvc 测试，测试里看到的路径可能不带 context path；实际运行时以服务启动配置为准。
- `IntentTreeController` 的更新、删除和批量接口是 `void` 返回，前端不要期待 JSON 体。
- `RAGChatController` 是核心流式入口，前端联调时务必按 SSE 方式处理。

---

## 18. 独立 MCP 服务

### 18.1 服务信息

- 服务名：`luoluo-rag-mcp`
- 默认端口：`9099`
- MCP 入口：`POST/GET /mcp`
- 传输方式：`HttpServletStreamableServerTransportProvider`
- 返回内容：MCP 协议格式，不是普通 REST JSON

### 18.2 服务说明

这个服务是独立于 Spring MVC REST 接口之外的 MCP Server，当前主要用于给大模型或 MCP Client 提供工具调用能力。

服务配置中注册了 3 个同步工具：

- `weather_query`
- `ticket_query`
- `sales_query`

### 18.3 `weather_query`

用途：查询天气信息，支持当前天气和未来天气预报。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `city` | string | 是 | 城市名 |
| `queryType` | string | 否 | `current` 或 `forecast`，默认 `current` |
| `days` | integer | 否 | 预报天数，仅 `forecast` 有效，默认 `3`，最大 `7` |

返回：

- `CallToolResult`
- 成功时 `isError=false`
- 数据为 `TextContent`

### 18.4 `ticket_query`

用途：查询工单数据，支持汇总、排名、明细和趋势分析。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `region` | string | 否 | 地区筛选 |
| `period` | string | 否 | 时间段，默认 `本月` |
| `product` | string | 否 | 产品筛选 |
| `salesPerson` | string | 否 | 工程师姓名筛选 |
| `queryType` | string | 否 | `summary`、`ranking`、`detail`、`trend`，默认 `summary` |
| `limit` | integer | 否 | 返回条数限制，默认 `10` |

### 18.5 `sales_query`

用途：查询销售数据，支持汇总、排名、明细和趋势分析。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `region` | string | 否 | 地区筛选 |
| `period` | string | 否 | 时间段，默认 `本月` |
| `product` | string | 否 | 产品筛选 |
| `salesPerson` | string | 否 | 销售姓名筛选 |
| `queryType` | string | 否 | `summary`、`ranking`、`detail`、`trend`，默认 `summary` |
| `limit` | integer | 否 | 返回条数限制，默认 `10` |

### 18.6 工具输出风格

- 这三个工具返回的是可直接给模型消费的纯文本总结。
- 不是结构化 JSON API。
- 如果调用参数缺失或非法，会返回 `isError=true` 的错误结果。
